# Deciduous backfill evidence

Historical graph backfills in this directory are frozen evidence ledgers paired with reviewable JSON snapshots under `.deciduous/exports/`.

Repository records remain authoritative. These backfills document consequential historical model changes and must not be used as current native Deciduous state, an alternate graph schema, or a recovery mechanism for live work.

Current preserved backfill:

- `factory-floor-archaeology.md` pairs with `.deciduous/exports/factory-floor-archaeology.json`.

Do not update the pair merely to mirror current Deciduous state. New development-history work uses Deciduous through upstream native interfaces as described in `AGENTS.md`.
