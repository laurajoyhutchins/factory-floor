{
  "name": "@factory-floor/control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/app.d.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "typecheck": "tsc -p tsconfig.json --pretty false",
    "test": "vitest run --root ../.. apps/control-plane/test/health.test.ts"
  },
  "dependencies": {
    "fastify": "^5.4.0",
    "@factory-floor/db": "workspace:*",
    "@factory-floor/runtime-core": "workspace:*",
    "yaml": "^2.9.0",
    "kysely": "^0.29.3"
  }
}
