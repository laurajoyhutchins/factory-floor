name: Format external action test

# Temporary branch-only workflow; removed after formatting lands.
on:
  push:
    branches:
      - agent/external-action-recovery

permissions:
  contents: write

concurrency:
  group: format-external-action-test
  cancel-in-progress: true

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: agent/external-action-recovery

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'

      - name: Format and commit
        run: |
          corepack prepare pnpm@10.12.1 --activate
          corepack enable
          pnpm install --frozen-lockfile
          pnpm exec prettier --write tests/integration/runtime-core/external-action-recovery.test.ts
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add tests/integration/runtime-core/external-action-recovery.test.ts
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'style: format external action recovery test'
          git push origin HEAD:agent/external-action-recovery
