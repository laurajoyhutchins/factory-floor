name: Format Task 12C Evidence Sanitizer

on:
  push:
    branches:
      - codex/close-milestone-1-acceptance-and-retain-evidence

permissions:
  contents: write

jobs:
  format:
    if: ${{ !contains(github.event.head_commit.message, '[task12c-sanitizer-formatted]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: codex/close-milestone-1-acceptance-and-retain-evidence

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Activate pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.12.1 --activate

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Format sanitizer
        run: pnpm exec prettier --write scripts/sanitize-m1-evidence.mjs

      - name: Commit formatted file
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add scripts/sanitize-m1-evidence.mjs
          git commit -m 'style: format evidence sanitizer [task12c-sanitizer-formatted]'
          git push origin HEAD:codex/close-milestone-1-acceptance-and-retain-evidence
