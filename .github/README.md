# Repository automation

`task1-verification.yml` is the authoritative recovery check for the Milestone 1 repository baseline. It resolves and locks dependencies, runs TypeScript and Python verification, validates Docker Compose, and requires PostgreSQL and MinIO to become healthy on a GitHub-hosted runner.
